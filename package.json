{
  "name": "crocodile-roulette",
  "version": "1.0.0",
  "description": "실시간 멀티플레이어 악어 이빨 뽑기 게임",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "npm run build:css && node server.js",
    "dev": "concurrently -n server,scss -c cyan,magenta \"nodemon --ignore client/css/ server.js\" \"sass --no-source-map --watch client/scss:client/css\"",
    "build:css": "sass --no-source-map client/scss:client/css"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "express": "^4.18.2",
    "express-session": "^1.19.0",
    "pug": "^3.0.4",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.14",
    "sass": "^1.98.0"
  }
}
