# 이 파일을 복사해서 .env로 저장한 뒤 값을 채우세요.
#   cp .env.sample .env

# (필수) 세션 서명에 사용되는 비밀 키. 임의의 긴 랜덤 문자열로 설정하세요.
# 생성 예: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=

# (선택) 서버 포트. 비워두면 4000번을 사용합니다.
PORT=4000
